#include "keyManager.h"

bool keyManager::keyDown[ALLEGRO_KEY_MAX] = {};

bool keyManager::keyUP[ALLEGRO_KEY_MAX] = {};

bool keyManager::isPress = false;

bool keyManager::press = false;

int keyManager::mouseX = 0;

int keyManager::mouseY = 0;

bool keyManager::mouseLeftDown = false;

bool keyManager::mouseRightDown = false;

void keyManager::Update()
{

}