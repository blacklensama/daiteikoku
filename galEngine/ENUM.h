#pragma once
enum gameStatue
{
	menu,
	galEngine,
	stop
};

enum galStatue
{
};

enum galString
{
	oneToOne,
	Finish
};

enum galStringManager
{
	history,
	now
};

enum PARSE
{
	drawbg,
	draw,
	clear,
	print,
	printEx,
	wait,
	bgm,
	voice,
	Goto,
	end
};

enum pic
{
	Style,
	finish
};